#include <iostream>

struct Vector2D{ //Vector 2D Struct
    double x;
    double y;

    Vector2D add(Vector2D other){ //returns the added vectors

    };

    Vector2D subtract(Vector2D other){ //returns this vector - other

    };

    double dotproduct(Vector2D other){ // returns the dot product of this vector and other vector

    };

    double getAngle(){ //returns the angle of the vector from +x in positive degrees
        
    };

    double getMagnitude(){ //returns the magnitude of this vector
        
    };
};